mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.thinker.service.entity
server:
    port: 8080
spring:
    application:
        name: thinker-service
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        password: root
        url: jdbc:mysql://127.0.0.1:3306/thinker?useUnicode=true&characterEncoding=utf-8
        username: root
    profiles:
        active: dev

# Glpr0p1!
